Build 170 bundles the official Cloudflare cloudflared Windows amd64 executable at this location during the verified Windows release workflow.

The binary is intentionally not committed to source control. CI validates source with this directory present; installer packaging replaces this file set with cloudflared.exe downloaded from the official Cloudflare GitHub release.
