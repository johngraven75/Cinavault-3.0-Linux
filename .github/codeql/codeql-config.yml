name: CinaVault Premium CodeQL configuration

# Vendored third-party GLib sources are scanned upstream and generate known
# pointer-model findings that are not actionable application-code defects.
# Keep all first-party Rust and TypeScript paths in the CodeQL analysis scope.
paths-ignore:
  - src-tauri/vendor/**
